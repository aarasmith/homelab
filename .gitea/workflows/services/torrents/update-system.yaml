name: services | torrents | update

on:
  workflow_dispatch:

jobs:
  update:
    uses: ./.gitea/workflows/common/lxc-ansible.yaml
    with:
      lxc_name: torrents
      playbook: ansible/common/update-docker-lxc.yaml
    secrets: inherit