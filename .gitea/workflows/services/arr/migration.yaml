name: services | arr | migration

on:
  workflow_dispatch:

jobs:
  migration:
    uses: ./.gitea/workflows/common/lxc-migration.yaml
    with:
      lxc_name: arr
      playbook: services/arr/migration.yaml
      source_host: "10.1.1.61"
      source_user: dietpi
    secrets: inherit