name: services | ombi | backup

on:
  workflow_dispatch:

jobs:
  start-lxc:
    uses: ./.gitea/workflows/common/lxc-start.yaml
    with:
      lxc_name: ombi
    secrets: inherit

  backup:
    needs: start-lxc
    uses: ./.gitea/workflows/common/lxc-ansible.yaml
    with:
      lxc_name: ombi
      playbook: ansible/common/backup-appdata.yaml
    secrets: inherit