name: Airflow LXC template CI/CD

on:
  push:
    branches:
      - main
      - dev
      - "feature/**"
    paths:
      - "ansible/airflow/airflow-template.yaml"
      - "ansible/airflow/docker-compose.yaml"
      - ".gitea/workflows/airflow/airflow-template.yaml"
  workflow_dispatch:

jobs:
  create-template:
    uses: ./.gitea/workflows/common/lxc-template.yaml
    with:
      playbook: services/airflow/ansible/airflow-template.yaml
      lxc_name: airflow
      template_name: airflow-debian12
      base_template: docker-debian12.tar.gz
      enable_docker: true
      unprivileged: true
    secrets: inherit