name: services | airflow | update

on:
  workflow_dispatch:

jobs:
  update:
    uses: ./.gitea/workflows/common/lxc-ansible.yaml
    with:
      lxc_name: airflow
      playbook: ansible/common/update-docker-lxc.yaml
    secrets: inherit